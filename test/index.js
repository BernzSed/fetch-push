import chai, { assert, expect } from 'chai';
import sinonChai from 'sinon-chai';
import sinon from 'sinon';
import buildFetch from '../src';

chai.use(sinonChai);

describe('fetch-push', () => {
  it.skip('will be build', () => {});
});
